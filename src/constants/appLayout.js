/**
 * Max widths for main /app tab content (coach, BRIDGE, tools).
 * Keeps typography readable on laptops without skinny centered columns.
 */
export const OA_CONTENT_MAX_PRIMARY = 960;

/** Benchmark / calculator style tabs — denser grids, slightly narrower */
export const OA_CONTENT_MAX_SECONDARY = 900;

/** Horizontal gutter on wide viewports — use inside padding shorthand */
export const OA_PAGE_PAD_X = "clamp(1rem, 5vw, 3rem)";
